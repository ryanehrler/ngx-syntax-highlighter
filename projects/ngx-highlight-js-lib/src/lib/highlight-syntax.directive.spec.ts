import { HighlightSyntaxDirective } from './highlight-syntax.directive';

describe('HighlightSyntaxDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightSyntaxDirective();
    expect(directive).toBeTruthy();
  });
});
